apiVersion: 1
datasources:
  - name: quakes
    type: frser-sqlite-datasource
    uid: quakes-ds
    access: proxy
    isDefault: true
    editable: true
    jsonData:
      path: /var/lib/grafana/data-ro/quakes.db
